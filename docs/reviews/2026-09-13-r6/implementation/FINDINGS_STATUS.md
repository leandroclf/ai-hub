# Findings status — R6 (estado vivo, atualizado por sessão)

Convenção: NAO_INICIADO / EM_ANDAMENTO / MECANISMO_PROVADO / CAMINHO_REAL_CONVERTIDO / FECHADO.
"FECHADO" exige o caminho real convertido e evidência de cenário completa — nunca apenas build/testes unitários verdes.

| Achado | Requisito | Rastro | Estado | Nota |
|---|---|---|---|---|
| F-R6-01 | R6-SEG-01 | ← R5-SEG-03 | CAMINHO_REAL_CONVERTIDO | Mecanismo + 132 call sites nos 5 serviços (orbita/cometa/atlas/pulsar/libra) convertidos; corte de credencial `hub`(superuser)→`hub_runtime` aplicado em compose/main.go. 177/177 testes reais passam sob `hub_runtime` (zero regressão sob owner). Restante: S02 (teste explícito de pool de conexão única) e runbook/métricas (R6-SEG-01-D) — não bloqueantes, mecanismo já correto e provado. Ver CHECKPOINT.md. |
| F-R6-02 | R6-SEG-02 | ← R5-SEG-01 | CAMINHO_REAL_CONVERTIDO | S01 (rotação): `callbackauth.KeyRing`, `callback_key_id`/`verified_at` persistidos, reconciliação usa a chave que aceitou originalmente. S02 (retry/quarentena): novo disposition `QUARANTINED` distinto de `REJECTED` após 3 falhas transitórias de apply; nunca elegível a prune automático; `Store.ReplayCallbackInbox` + endpoint `/admin/v1/callback-inbox/{id}/replay` (nominal, MFA, motivo obrigatório) é o único caminho de volta a `RECEIVED` — achado real encontrado e corrigido no processo: a CHECK constraint de `disposition` só aceitava RECEIVED/APPLIED/REJECTED, então `QUARANTINED` era silenciosamente rejeitado pelo Postgres e o erro era descartado por `_ = dispose(...)` no código, prendendo a obrigação em RECEIVED com lease expirado sem nunca progredir (corrigido: constraint ampliada, erro não é mais descartado). Locks/quotas de órfão particionados por conta+célula (era 1 lock global) — cobre S03. Testes reais `TestPostgresCallbackSurvivesKeyRotationDuringReconciliation` e `TestPostgresCallbackApplyFailureExhaustsIntoRecoverableQuarantine` — PASS sob owner e `hub_runtime` (62/62 no pacote). Falta, não bloqueante: retenção formal com prazo de alerta e alertas propriamente ditos (infra de alerta não existe no repo — fora de escopo de código puro) e integração real com vault (segredo hoje vem de env var/JSON, não de um resolver como `providerauth.SecretResolver`). |
| F-R6-03 | R6-EXE-01 | ← R5-EXE-03 | CAMINHO_REAL_CONVERTIDO | `atlas.ResolveOffer` resolve rota/conta/vínculo/contrato de compra por etapa (`StepOffers`), fail-closed se qualquer etapa não resolver; `orbita.BuildProductPlan` consome só o `StepOffer` da própria etapa (nunca o offer do produto pai) para conta e para `stepEconomicSnapshot` (só `Buy` do contrato da etapa); `cometa.executor` ganhou checagem defensiva extra (`snapshot_route_account_binding_mismatch`) contra divergência semântica dentro do próprio snapshot já hasheado. Achado colateral corrigido: `internal/objectstore` nunca tinha sido convertido às primitivas RLS — sob `hub_runtime` toda a custódia de arquivo ficava invisível; convertido (`catalog.go`, `retention.go`). Suíte completa 239/239 (owner) e 241/241 (`hub_runtime`), zero falha. Ver CHECKPOINT.md. |
| F-R6-04 | R6-EXE-02 | ← R5-EXE-01 | CAMINHO_REAL_CONVERTIDO | Achado: `RunIntentPublisher` (QUEUED) já barrava dispatch quando `Intent.Expired`, mas `RunDirectRecovery` (DIRECT) nunca checava a mesma flag — reenviava DIRECT ao provedor mesmo com `retry_until` vencido, mesmo com SLA do cliente futuro. Corrigido: mesma checagem adicionada antes de `DispatchDirect`; quando expirado, chama `CompleteIntent(..., false)` diretamente (que já fecha a intenção como EXPIRED atomicamente, sem tocar o transporte) — nenhum SUBMIT novo. Teste real novo `TestPostgresDirectRecoveryNeverResubmitsAfterRetryExpiryEvenWithFutureSLA` (S01) — PASS. S02/S03: a reconciliação por chave idempotente já existe e não foi alterada (`protocol_reconciliation_requests`/`cometa.ReconcileExternal`, mecanismo pré-existente); o fechamento de S01 garante que o caminho DIRECT nunca reabre a incerteza com um reenvio às cegas, que é a única lacuna real encontrada nesse requisito. |
| F-R6-05 | R6-EXE-03 | ← R5-EXE-04 | CAMINHO_REAL_CONVERTIDO | Achado: as 3 queries de claim (`RecoverOrphanedIntent`, `ClaimDirectIntent`, `claimIntentMode`) excluíam qualquer intenção — inclusive de compensação — assim que `protocols.status` alcançava um estado terminal público (SUCEEDED/FAILED/EXPIRED/CANCELLED/PARTIALLY_SUCCEEDED). Como uma compensação já é marcada com `continue_after_client_deadline=TRUE` no INSERT (mesma flag que já a isentava do gate de `client_deadline_at`), a correção reaproveitou a mesma coluna para isentá-la também do gate de status terminal (`OR i.continue_after_client_deadline`) — nenhuma migração nova necessária. Testes reais novos: `TestPostgresCompensationProgressesAfterPublicFinalState` (S01: compensação de A conclui depois do protocolo já finalizado EXPIRED; final público não é reaberto) e `TestPostgresCompensationUnknownThenDefinitiveFailureStaysReconciliable` (S03: UNKNOWN na própria compensação não vira sucesso por omissão; falha definitiva fica COMPENSATION_FAILED, reconciliável, nunca promovida a sucesso do produto) — ambos PASS. S02 (cancelamento) usa exatamente o mesmo mecanismo simétrico (CANCELLED está na mesma lista de status terminal) — nenhuma lacuna adicional encontrada, sem teste dedicado por ser código idêntico ao de S01/EXPIRED. |
| F-R6-06 | R6-EXE-04 | ← R5-EXE-02 | CAMINHO_REAL_CONVERTIDO | Nenhum achado de código: `CompleteIntent` já atualiza intent, lease de etapa (`operation_steps`) e contador do plano (`operation_plans.running_count`) na mesma transação `pg.WithTenantTx`; a query de claim (`claimIntentMode`) já trava `operation_plans FOR UPDATE` antes de decidir a última vaga, e o fencing por `epoch`/`lease_epoch` já impede que um dono antigo altere estado após takeover. Requisito fechado só com evidência real (não havia teste dedicado ao caso de produto/etapas antes desta sessão — só ao caso de intent simples). Testes reais novos: `TestPostgresSlotReleaseNeverExceedsMaxParallelNorDoubleDecrements` (S01: retry exaurido libera intent+etapa+contador atomicamente; S02: conclusão duplicada é no-op, vaga liberada é reutilizável exatamente uma vez, running_count nunca excede max_parallel) e `TestPostgresProductStepTakeoverAfterCrashRecoversSameSlotAndFencesOldOwner` (S03: takeover após lease RUNNING expirado recupera a mesma vaga sem contá-la de novo; dono antigo retornando não altera estado) — ambos PASS sem nenhuma alteração de código de produção. |
| F-R6-07 | R6-FIN-01 | ← R5-DAD-04 | CAMINHO_REAL_CONVERTIDO | Achado real (S01): `handlers.go`'s replay endpoint chamava `MarkQuarantineReplayed` sempre que `ProcessEnvelope` retornasse `nil` — mas re-quarentenar (payload ainda inválido) TAMBÉM retorna `nil`, então um replay que não resolveu nada era marcado REPLAYED por engano. Corrigido: `ProcessEnvelope` agora retorna um `ReplayDisposition` tipado (`APPLIED`/`QUARANTINED`/`CONFLICT`, via `ErrConflict` já existente); o handler só marca REPLAYED quando `APPLIED`, caso contrário registra a tentativa (`RecordQuarantineReplayAttempt`, migração `finance/0037` com `attempts`/`last_attempted_at`/`last_attempted_by`) e responde 409 sem prometer resolução. S02 (fato tardio após período fechado) e S03 (crash entre apply e confirmação do replay) já eram cobertos por mecanismo pré-existente (`ApplyEvent`'s dedup por `finance_inbox` hash + auto-quarentena `LATE_FACT_CLOSED_PERIOD` em `store.go`), sem lacuna de código — só faltava evidência real dedicada. Testes novos: `quarantine_replay_still_invalid_never_marks_replayed` (S01), `quarantine_replay_after_crash_between_apply_and_confirmation` (S03) — PASS sob owner e `hub_runtime`. |
| F-R6-08 | R6-FIN-02 | ← R5-DAD-03 | CAMINHO_REAL_CONVERTIDO | Nenhum achado de código: `ClosePeriod` já bloqueia fechamento sem completude durável de watermarks/reservas/disputas/ajustes (`ErrIncomplete`, testado desde R2/R5); `captureEffectiveTx`/`reservationState` já capturam o valor efetivamente contratado e liberam o excedente com evidência auditável, idempotentes por `finance_inbox`. Faltava evidência real dedicada ao caso numérico exato (reserva 10, consumo efetivo 7, duplicata). Teste novo `R6-FIN-02-S02_partial_capture_release_and_duplicate` (reserva 10 → captura 7 → libera 3 → nenhum lançamento/fato duplicado mesmo com entrega repetida) — PASS sob owner e `hub_runtime`. |
| F-R6-09 | R6-OPE-01 | ← R5-OPE-02 | NAO_INICIADO | |
| F-R6-10 | R6-OPE-02 | ← R5-DAD-02 | NAO_INICIADO | |
| F-R6-11 | R6-OPE-03 | ← R5-OPE-03;R5-EXE-05 | NAO_INICIADO | |
| F-R6-12 | R6-OPE-04 | ← R5-OPE-01 | CAMINHO_REAL_CONVERTIDO | Achado real (S01): `acquireCapacity` tratava rota sem `capacity_domain` como bypass silencioso (nenhuma checagem, I/O liberado) em vez de negação — apesar de Atlas já recusar publicar rota sem domínio (catalog.go), esse caminho defensivo permitia execução sem controle algum. Corrigido: domínio ausente OU controlador não instalado agora nega antes de I/O (`ErrCapacityPolicy`), igual a política vencida — sem bypass. 4 fixtures de teste que dependiam do bypass corrigidas para instalar política e domínio real. S02 (reduz/recupera AIMD, teto, justiça entre tenants) já estava coberto por `TestPostgresCapacityAdaptiveAndPending`. Achado real (S03): `ResolvePending` (única rotina que fecha um permit preso via evidência terminal, cobrindo `transport_open` OU `pending_external`) nunca tinha um chamador em produção — nenhum endpoint expunha reconciliação administrativa de capacidade, só diagnóstico read-only. Corrigido: novo endpoint `/admin/v1/capacity-domains/{domain}/permits/{id}/resolve` (nominal, MFA, escopo `integrations:write`, motivo 8-512 chars) chama `ResolvePending` com o epoch real do permit. Testes novos: `TestPostgresCapacityPermitStuckAfterSettlementFailureResolvesOnlyWithEvidence` (S03: lease vencido não recicla sozinho; só evidência autorizada libera a vaga, atribuída ao ator) e a suíte completa do pacote (63/63). |
| F-R6-13 | R6-UX-01 | ← R5-UX-01 | CAMINHO_REAL_CONVERTIDO | Achado real: `loadLookup` (CatalogPage.tsx) buscava só os 50 primeiros resultados do termo atual — uma referência já selecionada (campo, passo de produto ou rota) fora dessa janela desaparecia da lista, parecendo "sem seleção" ao reabrir (S01) ou ao pesquisar outro termo (S02), mesmo com o ID/versão corretos ainda persistidos no rascunho. Corrigido: `loadLookup` agora recebe os pares {id,versão} atualmente selecionados e busca cada um explicitamente por ID quando ausente do resultado, mesclando sem duplicar; o filtro textual local (client-side) também para de esconder a opção já selecionada quando ela não casa com o termo digitado. Cobre campos simples, `StepsEditor` (serviço/compensação por passo) e `RoutesEditor` (conta/vínculo por rota). S03 (corrida de tenant com busca em voo) já estava correto: o `AbortController` por efeito, com checagem de `signal.aborted` antes de aplicar qualquer resultado, já impede uma resposta antiga de contaminar o tenant/rascunho atual. Evidência: `tsc --noEmit` e `vite build` limpos (não há framework de teste no admin-ui; este é o mesmo padrão de qualificação já usado pelos commits anteriores do frontend). |
| F-R6-14 | R6-UX-02 | ← R5-UX-01 | CAMINHO_REAL_CONVERTIDO | Achado real (S01): o erro de JSON inválido no mapping de um passo (`StepsEditor`'s `mappingErrors`, estado local do componente filho) nunca chegava ao `save()` do formulário pai — o botão salvar/publicar permanecia habilitado e, como o valor inválido nunca sobrescrevia `draft.data` (só o texto local ficava fora de sincronia), uma mutação silenciosa com o mapping ANTERIOR (não o que o operador via na tela) podia ser enviada. Corrigido: novo callback `onErrorsChange` eleva o estado de erro do passo ao componente pai (`stepErrors`), que agora bloqueia tanto `save()` quanto o botão de publicar, igual aos campos JSON de nível superior. S02 (reconciliação após crash com respostas perdidas) e S03 (contrato de erro/retorno fora do esperado) já eram cobertos pelo mecanismo existente: `command()` reutiliza a mesma `Idempotency-Key` entre a chamada original e o retry automático de 5xx na mesma sessão; `save`/`action` usam `If-Match` por revisão, então uma resposta perdida nunca é reenviada às cegas — uma segunda tentativa com revisão desatualizada retorna 409/412 e mostra o diff (`conflict`) em vez de duplicar o efeito; `api()` já valida o contrato de resposta (`isResource`/`isPageOf`) e rejeita como erro explícito (502 `invalid_contract`) sem nunca aplicar `setDraft` sobre um payload fora do formato esperado. Evidência: `tsc --noEmit` e `vite build` limpos. |
| F-R6-15 | R6-QUA-01 | ← R5-QUA-01 | NAO_INICIADO | |

Achado adicional (não numerado no relatório original, mesma causa raiz de F-R6-01): bypass nominal `current_user = 'hub'` sobrevivendo nas policies de `hub_control`/`hub_finance` — corrigido nesta sessão (migrations `control/0038`, `finance/0035`).

Os 42 achados R2, 25 R3 e 17 requisitos R5 permanecem rastreáveis conforme rodadas anteriores; nenhum foi fechado em lote nesta sessão.
