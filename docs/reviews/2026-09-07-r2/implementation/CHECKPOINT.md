# Checkpoint R2

Execução em andamento; não concluída. HEAD inicial igual à revisão: `a39d394b0d87185ed4cc3861c12ec45f2c302d9e`. Branch de trabalho `r2-implementation`; sem commits R2 ainda.

Leitura integral do prompt de implementação realizada. Instruções locais AGENTS/CLAUDE e metodologia lidas. Principal leu changes 01/02/03 e documentos compartilhados; delegados leram suas changes (04/05, 06/07, 08/09), baseline e documentos pertinentes. Trechos longos de documentação com saída truncada devem ser relidos por fatias antes de concluir sua cobertura.

Três agentes foram interrompidos por limite de uso da plataforma e receberam retomada após o pedido do usuário. Seus arquivos em progresso são visíveis no workspace; não foram considerados entregas testadas. Há checkpoints específicos por frente.

Estado principal: pacotes auth e egress implementados com testes unitários aprovados; Atlas/Libra com middleware JWT e rotas administrativas montadas, clientes Atlas/Libra e dispatcher com credencial workload. Órbita deriva identidade do principal e resolve oferta publicada, valida input e grava snapshot/protocolo/intenção atomicamente. Migration core/0010 aplicada no laboratório. Teste PostgreSQL real `TestAdmissionPostgresAtomicIdempotency` passou: 24 concorrentes, conflito, aplicação independente, reserva protegida e rollback da intenção. Evidência `hub/evidence/r2/execution/admission-postgres.log`. Isto NÃO qualifica execução end-to-end.

Próxima ação principal: completar recuperação/claim de command_intents e reservas; Cometa ainda não implementa tentativa/posse/custódia/resposta real e não devolve `durable=true`, portanto dispatcher agora recusa afirmar custódia a partir do comportamento antigo. Finalização T-R2-01, DAG e Pulsar ainda pendentes. Configurar broker fora do bootstrap obrigatório, callbacks autenticados e filas com quarentena. Queue usa QUEUE_NAMESPACE (prefixo filas/tópicos) e cadeia AWS de credenciais fora de ENVIRONMENT=local; política SNS ainda precisa correção. Todos os três agentes voltaram a ser interrompidos por limite da plataforma, arquivos preservados. Podem ser retomados sem refazer trabalhos.

Comandos de laboratório: `docker compose -f hub/deploy/r2/compose.yaml ps`; `docker compose -f hub/deploy/r2/compose.yaml run --rm migrate`. Usar somente esse projeto; não alterar a stack anterior. Testes Go em `hub/`, frontend em `hub/admin-ui/`.

Nenhuma task de comportamento ou cenário foi aprovada nesta fase. P-01 a P-11 permanecem sem aprovação real; limites de uso de agentes não são bloqueio de requisito funcional. T-R2-01 exige experimento antes de concluir deadline; T-R2-04 exige limite agregado entre réplicas. Avançar frentes locais independentes.


Atualização da retomada (UTC 08/09 03:00): código adicional preservado:
- core/0011 operação com command/hash/tenant/cell, tentativa pregravada, observações e outbox atômicos; core/0012 quarentena; core/0013 identidade/tempo estáveis de outbox. Migrations aplicadas ao ai-hub-r2. Testes PostgreSQL em `execution/cometa-custody-postgres.log` passam 24 competidores/12 observações, replay real e tenant negativo.
- Publisher de command_intents QUEUED implementado com lease/epoch e recuperação, testado no DB; DIRECT e reserva pendente ainda precisam recuperação. Cometa executor usa PrepareSubmission e ConserveObservation para finais/UNKNOWN, resposta real synthetic-provider e egress protegido. PENDING, polling/callback/retry ainda precisam fechamento; não afirmar execução integral.
- Cometa middleware JWT e worker só ACK com Durable; quarentena de comando inválido precede ACK. Callback ainda cai em JWT e não está homologado. API operação interna ainda exige autorização de recurso além do middleware.
- Segredos: AWSVault usa SDK SigV4/GetSecretValue sem dependência nova; Basic/API key/OAuth resolvem valor, mTLS usa tls.X509KeyPair. Testes Basic/OAuth sem Redis e teste real LocalStack de cofre passaram (`provider-auth-unit.log`, `provider-vault-localstack.log`). mTLS ainda sem ensaio. Segredo sintético r2/provider-auth-test criado apenas no LocalStack.
- Atlas rotas legacy mutantes respondem 410; leituras exigem workload/escopo/célula. Offer verifica placement da célula. UI usa rotas versionadas.
- Representação final completa bytea/hash agora é gravada uma vez na finalização e retornada diretamente no GET/POST; falta teste integrado/Pulsar e T-R2-01 permanece SEM solução. Não liberar sucesso como qualificado.
- Requisitos/scenarios/findings inventariados em novos CSV/MD (164/205/42); nenhum PASS por inferência. Suíte Atlas PostgreSQL e suite Libra PostgreSQL passaram nesta retomada (`atlas-postgres.log`, `finance-postgres.log`), mas precisa revisão semântica dos testes antes de marcar cenários. `npm run build` passou TypeScript/Vite5.4.21; navegador não testado.
- Próximas frentes: contratos econômicos congelados no comando/fato, broker fora do bootstrap obrigatório, autenticação real provedor e polling/callback, deadline rigoroso, DAG, Pulsar, RLS/runtime, FileRef integração, APIs protocolos/SLA e UI/browser, kind/escala/restore. Não há motivo para concluir enquanto trabalho independente resta.


Atualização 08/09 03:10 UTC: Compose integrado subiu (bootstrap exit0), HTTP autenticado com Keycloak funcionou em Atlas/me/lista e sem token401 nas quatro APIs testadas. Chromium153 via Playwright1.63 instalado transitoriamente em /tmp/ai-hub-r2-browser (sem dependências novas do produto). Teste navegador identificou e corrigiu flex-direction row herdado em `.app-shell`; hipótese de propriedade única e repetição a390px passaram. Evidências `browser-layout-investigation.log`, `browser-smoke.log/json`, screenshot `admin-providers.png`. Login PKCE+OTP, lista backend, navegação e ausência de token persistente testados; logout apenas OBSERVED, não revogação completa. Arquivos de evidência indexados em `hub/evidence/r2/execution/EVIDENCE_INDEX.md`.

Em progresso adicional: API administrativa de protocolos/lista/detalhe/timeline e auditoria (`internal/orbita/admin.go`, migration0014); sem ensaio ainda. Bootstrap de broker movido para goroutine com retries em Orbita/Cometa/Libra/Pulsar; HTTP/timers não dependem de criar filas ao iniciar. Dispatcher possui SetQueueURL sincronizado. Contextos dos workers recebem SIGTERM. Teste host sem broker/Redis é próxima ação imediata (binários /tmp/ai-hub-r2-tools/{orbita,cometa}-offline em build). Timers Orbita e polling Cometa limitados por CELL_ID. Ainda falta teste shutdown completo e completar polling/recuperação.

Atualização da continuação: teste host `offline-startup.json` passou readiness em Órbita/Cometa sem broker/Redis. Pulsar recebeu implementação nova de custódia: migration0015, destinos por identidade/versão/secret_ref/versão, inbox+criação de entrega atômicos, representação bytea recebida em base64 no fato (não JSONB reconstruído), claim/epoch e tentativas pré-envio, HMAC com timestamp/event/delivery/body, quatro workers e recibo HTTP em transação. APIs admin destinos/entregas/reentrega com identidade/MFA; legacy mutation410. Teste real `pulsar-postgres.log` passou duas tenants na mesma URL/chaves diferentes, bytes originais, duplicatas, claims e HTTP204; não qualifica recuperação, fairness/carga ou todos os cenários. Runtime Compose ainda contém imagem anterior dessas últimas mudanças.

Contratos econômicos/aritmética foram extraídos para `internal/contracts/economics` (Libra conserva aliases e persistência). Publicação Atlas exige incidência e unidade explícitas; `economics.Freeze` congela compra/venda por versão e Orbita grava EconomicSnapshot na intenção. Finalizer inclui esse snapshot e evidence_id/occurred_at/cell no fato. Validação integrada financeira ainda pendente. OfferSnapshot agora contém conta de provedor versionada; Route tem provider_account_version. Cometa resolve binding publicado exato por endpoint novo `/v1/credentials/binding/{id}/{version}` com revogação/validade/tenant/cell; usa conta congelada, não contrato legacy para selecionar credencial. UI ganhou unidade, incidência, reserva explícita e campo de regras de plano. Compilação local passou; testes -race com Postgres e build UI em andamento.

PRÓXIMO: recolher sessões de teste; implementar/qualificar fluxo sintético HTTP integrado (catálogo/offer/Cometa/final/Pulsar/Libra), resolver T-R2-01 sem relaxar requisito, completar receipt/poll/callback/UNKNOWN/retry/capacidade/DAG, FileRef, RLS, jornadas admin/SLA e kind/carga/restauração. Ainda NÃO há rodada concluída nem bloqueio total.

Atualização da continuação em 08/09/2026 16:30 UTC:
- Consumidor Orbita `operation.observed` foi refeito com validação de envelope/produtor/tenant/aplicação/célula/operação, inbox durável `core/0016`, quarentena e ACK somente após consolidação. PostgreSQL real + race 3 repetições passou; falha de persistência deixa recibo RECEIVED e reentrega é segura.
- Cometa `PENDING` agora exige custódia transacional de correlação, recibo, agenda de polling, resultado e outbox. Consulta interna exige workload, escopo `protocols:read`, tenant/aplicação e célula; leitura com resultado NULL foi corrigida. PostgreSQL real + race passou.
- AUTO usa `auto_wait_seconds` publicado, espera apenas a autoridade local e conserva intenção sem renovar prazo; teste PostgreSQL passou nos casos rápido, lento, retry e desconexão. `catalog.ValidatePublication` agora exige espera AUTO válida.
- Polling ganhou política congelada no snapshot, deadline absoluto, claim/status com epoch/lease/tentativa, egress/auth atual e custódia callback/poll concorrente. Migrations 0017 e aditiva 0019 aplicadas com checksum. PostgreSQL real + race passou em 24 concorrentes, takeover, stale fence, backoff e HTTP autenticado.
- CapacityController PostgreSQL implementa limite agregado por domínio, reservas tenant/célula, rate rolling, pendência UNKNOWN, fencing, reconciliação positiva e AIMD. Teste real passou com 60 concorrentes, duas células e três identidades. A API ainda não está integrada ao Executor; política é fixture, não homologação.
- Contratos `api/openapi.yaml`, `api/openapi-internal.yaml` e `api/asyncapi.yaml` foram alinhados à identidade OIDC e às identidades de aplicação/célula; a documentação antiga de `X-Tenant-Id` foi removida.
- Suítes `go test -race` de Cometa/Órbita/Pulsar/Libra passaram; build Vite passou. T-R2-01 permanece aberto: a checagem pré-commit atual não prova confirmação durável estritamente anterior ao deadline.
- `go test ./...`, `go vet ./...`, `git diff --check` e `npm run build` passaram após a integração. A imagem atual dos cinco serviços foi construída e executada; `/healthz/ready` respondeu 200 em 18080–18084. A reconstrução completa ficou parcialmente bloqueada por timeout TLS ao baixar `grafana/loki:3.3.2` do Docker Hub (`compose-bootstrap-current.log`); observabilidade não foi declarada comprovada.
- O ensaio HTTP autenticado atual não pôde emitir token: a instância Keycloak importada no volume persistente respondeu `invalid_user_credentials` para a fixture `operadora-a`, embora os arquivos de realm contenham a senha sintética. Isso é bloqueio do fixture/volume local, não aprovação de autenticação; respostas sem token continuam 401 onde a rota existe. Não resetar o volume sem preservar diagnóstico.
